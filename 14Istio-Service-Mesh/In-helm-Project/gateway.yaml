apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: smc-gateway
  namespace: dev

spec:
  gatewayClassName: istio

  listeners:
    - name: http
      protocol: HTTP
      port: 80

      hostname: smc.local

      allowedRoutes:
        namespaces:
          from: Same